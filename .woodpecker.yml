when:
  - event: [push, pull_request, manual]
    branch: main
  - event: [manual]
    branch: [main, "v*"]

steps:
  build:
    image: golang:latest
    commands:
      - make cross

  test:
    image: golang:latest
    commands:
      - go test ./...
